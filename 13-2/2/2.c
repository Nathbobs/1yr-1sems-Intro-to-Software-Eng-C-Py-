#include <stdio.h>
#include <string.h>

int main()
{
    char words[20];
    char eng[10];
    int i,j,n;
    int count=0;
    scanf("%s \n", eng);
    for(i=0;i<10;i++)
        for(j=0;j<20;j++ )
        {
            while (eng != "end");
            continue;
        }
}