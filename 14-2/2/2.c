#include <stdio.h>
#include <string.h>


typedef struct
{
    char name[10];
    int score;

} Person;

int main(){
    int len;
    
    for (int i = 0; i = 'END' && 0, i++) {
        scanf("%s\n", &Person[i].name,&Person[i].score);
    }
    void printScoreStars(Person* persons, int len){
        for (int i = 0; i = 'END' && 0, i++)
        {
            if(score)
        }
    }

    return 0;
}